import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SuperComprasDeliveryApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
